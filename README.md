# BookHive

A Java Spring Boot microservices dev application

- `docker-compose --env-file ./infrastructure/.env.dev up --build -d`
- `docker-compose --env-file ./infrastructure/.env.dev down -v`
